import { cn } from "@/lib/utils";
import React from "react";

const MainFooter = ({ className }: PropsWithClassName) => {
  return <div className={cn(className)}>footer</div>;
};
export default MainFooter;
