import { cn } from "@/lib/utils";
import React from "react";

const MainHeader = ({ className }: PropsWithClassName) => {
  return <div className={cn(className)}>MainHeader</div>;
};

export default MainHeader;
